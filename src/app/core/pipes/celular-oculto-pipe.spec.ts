import { CelularOcultoPipe } from './celular-oculto-pipe';

describe('CelularOcultoPipe', () => {
  it('create an instance', () => {
    const pipe = new CelularOcultoPipe();
    expect(pipe).toBeTruthy();
  });
});
